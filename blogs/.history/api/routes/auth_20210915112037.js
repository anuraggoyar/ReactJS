const router = require('express').Router();
const User = require('../models/User');

//Register User :
router.post('/register', async (req, res) => {
    try{

    } catch(error) {
        res.sta
    }
});
//Login User :


